Run all tests in the BlazingBudget solution and report results.

## Instructions

1. Run all tests using: `dotnet test BlazingBudget.sln`
2. Report a summary of passed/failed tests
3. For any failures, show the test name and error message
4. Suggest fixes for failing tests if the cause is apparent

## Test Projects

- `tests/BlazingBudget.Domain.Tests` - Domain layer unit tests
- `tests/BlazingBudget.Application.Tests` - Application layer tests
- `tests/BlazingBudget.TUnitTesting.Tests` - TUnit-based tests
